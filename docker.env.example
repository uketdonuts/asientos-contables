# Configuración de Base de Datos MySQL
DB_NAME=asientos_contables
DB_USER=asientos_user
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD_32_CHARS
MYSQL_ROOT_PASSWORD=CHANGE_ME_STRONG_ROOT_PASSWORD_32_CHARS

# URL de conexión para Django
DATABASE_URL=mysql://asientos_user:CHANGE_ME_STRONG_PASSWORD_32_CHARS@db:3306/asientos_contables

# Configuración general
DEBUG=0
SECRET_KEY=CHANGE_ME_DJANGO_SECRET_KEY_50_CHARS

# Configuración de Email
EMAIL_HOST=mailhog
EMAIL_PORT=1025
EMAIL_USE_TLS=False
EMAIL_USE_SSL=False
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=Sistema Contable <noreply@asientos-contables.local> 

# TEMP: Bypass 2FA (1/0 or true/false). Disable after troubleshooting.
TWO_FACTOR_BYPASS=0

# INSTRUCCIONES:
# 1. Copia este archivo como 'docker.env'
# 2. Genera contraseñas seguras con:
#    python -c "import secrets; import string; print(''.join(secrets.choice(string.ascii_letters + string.digits + '!@#$%^&*()-_=+') for _ in range(32)))"
# 3. Reemplaza todos los valores CHANGE_ME con tus contraseñas reales
# 4. NUNCA subas el archivo docker.env a git
